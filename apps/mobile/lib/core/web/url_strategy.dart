/// Non-web stub: nothing to configure.
///
/// The web implementation lives in `url_strategy_web.dart` and is selected by
/// a conditional import in `main.dart` so web-only packages (`flutter_web_plugins`,
/// which transitively pulls in `dart:ui_web`) never compile for Android/iOS.
void configureUrlStrategy() {}